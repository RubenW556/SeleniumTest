node {
    stage "Create build output"
    checkout scm
    bat "dir"
    bat "path"
    bat "python --version"
    bat "pip --version"
    bat "python -m venv ./venv"
    bat "./venv/bin/activate"
    bat " pip install -r ./requirements.txt pytest"
    bat "--junitxml report.xml"
}